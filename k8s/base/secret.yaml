apiVersion: v1
kind: Secret
metadata:
  name: messaging-service-secret
  labels:
    app: messaging-service
    app.kubernetes.io/name: messaging-service
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: whispr
type: Opaque
stringData:
  # IMPORTANT: Replace these values in production!
  # Use sealed-secrets or external-secrets-operator
  DB_USERNAME: "messaging_service"
  DB_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  SECRET_KEY_BASE: "CHANGE_ME_MINIMUM_64_CHARS_REQUIRED_FOR_PHOENIX_SECRET_KEY_BASE"
  REDIS_PASSWORD: ""
